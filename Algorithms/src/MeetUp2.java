
public class MeetUp2 {

	// Keep track of balance of left and right paranthesis, brackets, curly braces
	// Push left characters into stack. When encountering right character, pop from stack and compare.
	// Throw an exception where the imbalance is instead of returning false -> keep two stacks. One with left characters and index in string
	String input = "func(hello[eilsils], <)>, 1484839)";
	
	
}

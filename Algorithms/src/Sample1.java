
public class Sample1 {

	Sample1()
	{
		
	}
		
    public String solution(String S, int K) {
        
    		S = S.toUpperCase();
  
    		if(S.length() == 1) return S;
    		
    		S = S.replace("-", "");   
    		
    		int lenght = S.length();
	    		 
	}
    
    private addChar(String S, int K, int length)
    {
    
    		boolean isEven = length%2 == 0;
    		
    		if(length % K != 0)
    		{
    			int grouLength = (int) Math.ceil(length/K);
    		}
    	
    }
	
	
}
